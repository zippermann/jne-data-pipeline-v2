FROM apache/airflow:2.8.0-python3.11

# Install packages as airflow user
USER airflow

# Bronze extraction dependencies
RUN pip install --no-cache-dir \
    pyarrow \
    pyyaml \
    oracledb \
    minio
